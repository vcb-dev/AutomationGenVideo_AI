# Django Core
django>=4.2.0
djangorestframework>=3.14.0
drf-spectacular>=0.27.2
django-cors-headers>=4.0.0
django-environ>=0.10.0
PyJWT>=2.8.0

# Database
psycopg2-binary>=2.9.9

# Task Queue
celery>=5.3.0
redis>=5.0.0

# HTTP & API
requests>=2.31.0
httpx>=0.27.0

# AI APIs
openai>=1.0.0
google-generativeai>=0.8.0
anthropic>=0.25.0

# Document Processing
pypdf>=4.0.0

# Web Scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Retry & Resilience
tenacity>=8.2.3

# Environment
python-dotenv>=1.0.0

# Server
gunicorn>=21.2.0

# Utilities
unidecode>=1.3.0
python-dateutil>=2.8.2
pytz>=2024.1
# Excel Processing
openpyxl>=3.1.0
pandas>=2.0.0

# Deep Learning & Video Processing
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# HeyGen API (AI Avatar & Lipsync)
aiohttp>=3.9.0
ffmpeg-python>=0.2.0
moviepy>=1.0.3

# Video Download & Analysis
# Cài trực tiếp từ GitHub master (thay vì PyPI) để có fix extractor mới nhất — yt-dlp
# thường vá lỗi bị nền tảng chặn/đổi API nhanh hơn nhiều so với chu kỳ release PyPI.
# Ghim theo commit cụ thể để cài lại (requirements install) vẫn tái lập được, không bị
# trôi theo master mỗi lần install mới.
yt-dlp @ git+https://github.com/yt-dlp/yt-dlp.git@aefce1eea4

# Douyin bắt buộc chữ ký JS cho API chi tiết video, yt-dlp không tự tính được — dùng
# Chromium headless để chính trang tự gọi API đó (xem video_management/scripts/douyin_extract.py).
# Sau khi cài, chạy thêm: playwright install chromium (tải trình duyệt, ~280MB, 1 lần).
playwright>=1.61.0
playwright-stealth>=2.0.3
